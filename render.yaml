services:
  - type: web
    name: khoja-ahmed-yasawi-backend
    runtime: node
    plan: free
    buildCommand: npm install && pip3 install -r requirements.txt
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
